#Fail a pipeline
name: example07
on: [push]
jobs:
  # Sequential Jobs
  Test1:
    runs-on: ubuntu-latest
    steps:
      - run: |
          #echo "::error Intentional Error ::Missing semicolon"
          #exit 1
          echo "::Summary 100 resource created ::Missing semicolon"